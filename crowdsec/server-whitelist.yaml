name: server-ip-whitelist
description: Whitelist server and admin IPs to prevent banning from internal traffic
whitelist:
  reason: Server and admin traffic
  ip:
    - ${SERVER_IP}
    - 185.209.196.213
    - 88.130.156.202 # dev/sync box IPv4
  cidr:
    - 2001:9e8:5be1:1800::/64 # dev/sync box IPv6 /64 (new-api-sync trips http-probing/403-bf on its own high volume)
