name: server-ip-whitelist
description: Whitelist server and admin IPs to prevent banning from internal traffic
whitelist:
  reason: Server and admin traffic
  ip:
    - ${SERVER_IP}
    - 185.209.196.213
